# Copyright (c) PRO-Robotech
# SPDX-License-Identifier: BUSL-1.1

apiVersion: v2
name: api-gateway
version: 1.2.0
appVersion: 1.2.0
description: Kachō API Gateway — cmux + gRPC-proxy + grpc-gateway REST + allowlist + OpsProxy
