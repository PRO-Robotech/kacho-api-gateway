# Go build
*.exe
*.test
*.out
bin/
build/
dist/
coverage.out
# in-tree protoc plugin binary (built into a temp dir by
# scripts/gen-permission-catalog.sh; a stray `go build ./cmd/...` lands here)
/protoc-gen-kacho-permissions

# IDE
.idea/
.vscode/
.DS_Store
go.work
go.work.sum

# AI-оснастка (источник истины — kacho-workspace, локально синкается) — не коммитим
.claude/
CLAUDE.md
AGENTS.md
.codex/
.agents/
